# RuleQuant 试用说明（先看我）

## 这是什么

RuleQuant 是一个“规则型开奖数据公式回测系统 / RuleQuant 回测终端”。

它只做：

- 历史开奖数据分析；
- 用户提供公式计算；
- 逐期明细核对；
- 历史回测；
- 综合参考排序；
- 公式库备份和恢复。

它不做：

- 不保证结果；
- 不承诺必中；
- 不提供投注建议；
- 不接入支付、资金或下单流程。

页面里的“综合参考结果”“参考号码”“参考生肖”都只用于公式研究和参考排序。

## 运行环境

对方电脑需要先安装：

- Node.js 20 或更高版本；
- 浏览器，推荐 Chrome 或 Edge。

如果没有 Node.js，请先到 https://nodejs.org/ 安装 LTS 版本。

## Windows 快速启动

1. 解压压缩包；
2. 打开解压后的文件夹；
3. 双击 `start-rulequant-windows.bat`；
4. 脚本会自动选择可用端口并打开浏览器；
5. 如果浏览器没有自动打开，请按窗口里显示的地址手动打开。

默认地址通常是：

```text
http://localhost:3000/dashboard
```

如果 3000 被占用，脚本会自动尝试 3001、3002、3003、3004。

如果脚本提示没有 Node.js，请先安装 Node.js 后再运行。

## 命令行启动

在项目目录打开终端，运行：

```bash
corepack enable
pnpm install
pnpm build
pnpm start
```

然后打开：

```text
http://localhost:3000/dashboard
```

如果没有 pnpm，也可以先运行：

```bash
npm install -g pnpm
```

## 普通用户试用流程

首页已经按三步设计：

1. 同步开奖数据；
2. 一键算公式；
3. 查看综合参考结果。

建议按这个顺序试用，不要一上来先改公式。

## 1. 同步开奖数据

进入首页、开奖数据页或“综合参考结果”页，点击“同步最新开奖数据”或“同步并写入本地库”。

系统也会在打开核心页面时按当天自动同步一次。网站每天更新后，重新打开软件或点击同步按钮，会把网站新数据写入本地开奖库，不只是临时预览。

页面会显示：

- 最新期号；
- 最新开奖号码；
- 数据来源；
- 已同步记录数；
- 最后同步时间；
- 是否使用最新同步数据。

如果页面提示“当前可能不是最新开奖数据，请先同步”，请先同步再看综合参考结果。

## 2. 一键算公式

进入“一键算公式”页。

可以选择：

- 使用最新同步开奖；
- 手动输入一期开奖号码。

点击“一键计算全部公式”后，每条启用公式会显示本期计算结果。点“查看明细”可以进入公式逐期明细。

## 3. 查看综合参考结果

进入“综合参考结果”页，点击“重新生成综合参考结果”。

页面顶部会显示：

- 使用的最新期号；
- 最新开奖号码；
- 数据来源；
- 启用公式数量；
- 可计算公式数量；
- 实际参与公式数量；
- 样例已核对 / 未核对数量；
- 本次生成证据数量；
- 结果生成时间。

如果没有可参与公式，系统不会展示假的 Top 号码，会提示先检查公式状态。

## 4. 点击号码看依据

在“综合参考结果”页点击 Top 号码或 Top 生肖，右侧会显示：

- 为什么入选；
- 哪些公式支持；
- 哪些公式反对；
- 每条公式本期计算过程；
- 公式历史表现；
- 公式来源；
- 是否用户提供公式。

没有任何证据的号码不会进入 Top 结果。

## 5. 查看公式逐期明细

进入“公式逐期明细”页，可以看到每一期完整流水账：

- 当前期号；
- 当前期开奖号码；
- 变量取值；
- 完整计算过程；
- 原始结果；
- 归一化过程；
- 映射成生肖、尾数、合数、段位等；
- 最终输出；
- 下一期开奖；
- 正确或错误。

错期会明显标出来。

明细里的变量取值、公式原文、原始结果、归一化过程默认展开，方便直接对账，不需要每期都手动点开。

如果最新一期已经同步，但还没有下一期开奖，系统也会把最新一期显示出来，并标记为“待验证”。这表示该期已经加入数据并完成公式计算，只是要等下一期出来后才能判断正确或错误。

## 性能和启动说明

本包已按普通试用优化：

- 启动脚本默认使用 Next.js 生产模式，不再用开发模式启动；
- 压缩包内包含已生成的生产构建，正常情况下不用每次启动都重新编译；
- 页面切换时只计算当前页面需要的数据，不在所有页面都跑全量公式回测；
- 最新一期逐期明细会按需生成“待验证”记录，不影响历史成功率统计；
- 公式逐期明细按需加载，默认先显示最近 20 期，可继续加载更早数据；
- 一键算公式、综合参考结果、公式筛选等重计算页面有独立计算入口和状态提示；
- 开奖数据页、设置页、帮助页不会后台跑综合参考结果。

## 6. 新增公式

进入“公式管理”，点击“新增公式”。

新增模式会进入空白表单，保存按钮显示“保存为新公式”，不会覆盖旧公式。

新增前可以点击“试算当前公式”，系统会直接计算当前输入框里的草稿公式，不需要先保存。

## 7. 公式来源说明

每条公式有来源：

- 用户提供公式：默认可参与综合参考；
- 人工新增公式：用户自行决定是否参与；
- 系统推荐公式：默认不参与，确认加入后才参与；
- 示例公式：不能混进正式综合结果。

样例未核对不等于不能用。用户提供公式只要已启用、可计算、没有变量识别错误，就可以参与综合参考。

## 8. 异常公式

“异常公式集中显示”会列出不能参与的公式和原因，比如：

- 变量未识别；
- 公式类型缺失；
- L序 / D序缺失；
- 取位配置缺失；
- 计算结果无法归一化；
- 含未确认的“位”变量；
- 用户手动退出综合参考。

点击“编辑修复”可以直接进入公式编辑器。

## 9. 公式库备份

“公式管理”页提供：

- 导出当前公式库 JSON；
- 导入公式库 JSON；
- 重置为内置公式；
- 恢复上一次公式库；
- 修改公式前自动备份。

建议试用者在大量修改前先导出一次公式库 JSON。

## 10. 运行记录 / 操作日志

首页、综合参考页和部分详情页会显示运行记录。

记录内容包括：

- 什么时候同步开奖数据；
- 同步到哪一期；
- 什么时候重新生成综合参考结果；
- 使用多少期数据；
- 多少条公式参与计算；
- 生成多少条公式依据；
- 哪些公式被停用、新增、修改、删除；
- 哪些公式计算异常。

这方便后续反馈问题。

## 反馈问题时请发这些信息

如果试用中发现问题，请截图或复制：

- 页面地址，例如 `/candidate-pool`；
- 最新期号和开奖号码；
- 实际参与公式数量；
- 本次生成证据数量；
- 出问题的公式名称；
- 公式逐期明细里的那一期计算过程；
- 操作日志中最近几条记录。

## 已验证

本次打包前已验证：

- 自动测试通过：42 / 42；
- TypeScript 类型检查通过；
- Next.js 生产构建通过；
- 浏览器检查 `/dashboard`、`/candidate-pool`、`/rules`、`/formula-editor?mode=new`、`/one-click` 无红屏、无 Hydration Error；
- 综合参考页实际参与公式 33 条，生成公式依据 34 条；
- 一键计算和公式编辑器草稿试算均可正常运行。
